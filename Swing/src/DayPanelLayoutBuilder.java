/*
import javax.swing.*;

import java.awt.*;

import static java.awt.Color.WHITE;

public class DayPanelLayoutBuilder extends JFrame {

    JPanel dayPanel;
    int i;
    int weekdayValue;

    public DayPanelLayoutBuilder(JPanel dayPanel, int i, int weekdayValue) {
        this.dayPanel = dayPanel;
        this.i = i;
        this.weekdayValue = weekdayValue;
    }

    public JPanel setDayPanelLayout() {
        dayPanel.setBackground(WHITE);
        if (i == weekdayValue) {
            dayPanel.setBackground(Color.lightGray);
        }
        dayPanel.setLayout(new GridLayout(12, 1));
        return dayPanel;
    }

    protected JPanel getDayPanel() {
        return dayPanel;
    }
}
*/
